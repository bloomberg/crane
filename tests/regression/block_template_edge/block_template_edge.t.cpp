#include <block_template_edge.h>

int main() {
    // Just test that these compile correctly.
    // The block template tests require stdin interaction,
    // so we just verify compilation.
    return 0;
}
