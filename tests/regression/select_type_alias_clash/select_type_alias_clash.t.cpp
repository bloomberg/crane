#include <select_type_alias_clash.h>

#include <cassert>
#include <iostream>

int main() {
  assert(answer == 42);
  std::cout << "All select_type_alias_clash tests passed!" << std::endl;
  return 0;
}
