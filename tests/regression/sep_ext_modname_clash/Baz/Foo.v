From Crane Require Import Extraction.
From Crane.Mapping Require Import Std.
Require Import Crane.Mapping.NatIntStd.

Definition baz_add (n m : nat) : nat := n + m + 1.
Definition baz_only (n : nat) : nat := n + 2.
